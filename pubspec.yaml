name: product_hunt_app
description: Product Hunt App built using Product Hunt's API
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_bloc: ^7.2.0
  internet_connection_checker: ^0.0.1+2
  intl: ^0.17.0
  json_annotation: any
  path: ^1.8.0
  path_provider: ^2.0.3
  retrofit: ^2.0.1

dev_dependencies:
  build_runner: ^2.1.1
  flutter_test:
    sdk: flutter
  json_serializable: any
  retrofit_generator: any
  very_good_analysis: ^2.3.0

flutter:
  uses-material-design: true
